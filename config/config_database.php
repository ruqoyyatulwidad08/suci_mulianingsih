<?php
require "config-global.php";
fucntion($host, $nama, $alamat,$db) {
    koneksi(hostname, alamat, password, database)
    $koneksi = new mysql($host, $user, $password, $db);
    return $koneksi;
}

